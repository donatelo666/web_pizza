<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Panel de Ordenes</title>

    <!--css , fuentes , bootstrap y color-->
    <link rel="stylesheet" href="/css/promociones.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Lemon&family=Sigmar&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
      rel="stylesheet"
      crossorigin="anonymous"
    />

    <style>
      body {
        background-color: #58c06e;
      }
    </style>
  </head>

  <body>
    <!--navbar de partials-->
    <%- include('../partials/navbarAdmin') %>

    <!--contenedor de tarjetas que recorre cada promocion y crea con boton editar y eliminar-->
    <div class="contenedor-tarjetas">
      <% promociones.forEach(promo => { %>
      <div class="tarjeta-elegante">
        <h4><%= promo.titulo %></h4>
        <p><%= promo.descripcion %></p>

        <a
          href="/admin/promociones/editar/<%= promo.id %>#formEditarPromo"
          class="btn btn-warning btn-sm"
        >
          ✏️ Editar
        </a>

        <form
          action="/admin/promociones/<%= promo.id %>?_method=DELETE"
          method="POST"
          class="d-inline"
        >
          <button
            type="submit"
            class="btn btn-danger btn-sm"
            onclick="return confirmarEliminacion('¿Eliminar esta promoción?')"
          >
            🗑️ Borrar
          </button>
        </form>
      </div>
      <% }) %>
    </div>

    <!--formulario doble para crear promo o editar con autollenado de los datos de promos,
     boton guardar -->
    <hr class="text-white" />
    <h3 class="text-white">
      <%= promoParaEditar ? "✏️ Editar promoción" : "➕ Nueva promoción" %>
    </h3>

    <form
      action="<%= promoParaEditar ? '/admin/promociones/' + promoParaEditar.id + '?_method=PUT' : '/admin/promociones/crear' %>"
      method="POST"
      class="bg-dark p-3 rounded text-white"
      id="formEditarPromo"
    >
      <div class="mb-3">
        <label>Título</label>
        <input
          type="text"
          name="titulo"
          value="<%= promoParaEditar?.titulo || '' %>"
          class="form-control"
          required
        />
      </div>
      <div class="mb-3">
        <label>Descripción</label>
        <textarea name="descripcion" class="form-control" required>
<%= promoParaEditar?.descripcion || '' %></textarea
        >
      </div>

      <button type="submit" class="btn btn-success">💾 Guardar</button>
    </form>

    <% if (mensaje) { %>
    <script>
      alert("<%= mensaje %>");
      document.getElementById("formEditarPromo")?.reset();
    </script>
    <% } %>

    <!--alert confirmacion de borrado-->

    <script>
      function confirmarEliminacion(mensaje) {
        const confirmado = confirm(mensaje);
        if (confirmado) {
          alert("✅ Promoción eliminada con éxito");
          return true; // permite que el formulario se envíe
        } else {
          alert("❌ Eliminación cancelada");
          return false; // evita que se envíe el formulario
        }
      }
    </script>

    <!--cerra con exito sesion -->
    <div id="notificacion" class="oculto"></div>

    <!--script log out -->
    <script src="/js/logout.js"></script>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
